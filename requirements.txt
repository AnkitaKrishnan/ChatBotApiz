# Core libraries
torch==2.0.1  # For deep learning and model training
transformers==4.31.0  # Hugging Face Transformers for model loading and fine-tuning
datasets==2.12.0  # Hugging Face Datasets for loading and processing datasets
accelerate==0.21.0  # For distributed model training

# API libraries (optional, but necessary if you want to serve the model)
Flask==2.3.3  # For building a simple API (use FastAPI if preferred)
FastAPI==0.98.0  # Optional: If you prefer FastAPI for the API instead of Flask
uvicorn==0.22.0  # Required if using FastAPI for running the server

# Miscellaneous
scikit-learn==1.2.2  # For evaluation metrics (if needed for classification tasks)
numpy==1.24.3  # For numerical operations
pandas==1.5.3  # For handling dataframes
matplotlib==3.7.2  # For plotting and visualizing data (if needed)

# For interactive web-based UI (Optional)
gradio==3.31.0  # If you want to use Gradio for creating a UI to interact with the model

# You might also want to include these if you're doing token-based login for Hugging Face:
huggingface_hub==0.15.0  # Hugging Face Hub integration for loading models/datasets

-U datasets
safetensors
sentencepiece
torchvision 
torchaudio


